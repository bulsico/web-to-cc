# Directory where Claude Code will run (required)
CC_PROJECT_DIR=/home/user/my-project

# Path to the claude binary. Defaults to searching PATH, then ~/.local/bin/claude
# CC_BINARY=claude

# Optional password to protect the web UI (HTTP Basic Auth).
# Leave empty to disable authentication.
# CC_PASSWORD=your-secret-password
